function return_data = test_basin(bestfit)

    gap = bestfit-[0,bestfit(1:end-1)];
    gap = gap(2:end);
    half_gap = gap/2;

    basin(1) = bestfit(1) + half_gap(1)
    basin(i) = half_gap(i-1)

    return_data = 0;

end